# Build artifacts
/gmessages
/gmessages-demo
/genviz
/om-bin
.build/

# Local Windows build artifacts (gmessages.exe / gmessages-full.exe)
/gmessages*.exe

# Local data / secrets
*.db
session.json
.env*.local

# Editor / tooling
.claude/launch.json

# Node / Playwright (web UI e2e tests)
node_modules/
playwright-report/
test-results/
.playwright-cli/
/bun.lock
output/

# Local Go workspace overrides — use a local checkout of dependencies
# without committing it. CI/release builds use the versions in go.mod.
go.work
go.work.sum

# One-off recovery dumps that should never be tracked
/cc.json
/.tmp-*.sql
